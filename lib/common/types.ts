/**
 * Shared type definitions used across PDF and Office modules
 */

/**
 * ISO 639-3 language code (as output by franc), or "und" for undetermined.
 * @example 'eng', 'jpn', 'deu', 'und'
 */
export type Lang = string
